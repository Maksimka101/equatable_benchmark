name: equatable_benchmark
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.2.0

# Add regular dependencies here.
dependencies:
  benchmarking: ^0.6.1
  
  equatable:
    git: 
      url: git@github.com:Maksimka101/equatable.git
      # Comment out the line below to test with the master branch.
      ref: feat/performance-improvement

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
